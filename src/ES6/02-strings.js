let hello = 'Hello'
let world = 'World'

let epicPhrase = hello + ' ' + world + '!'
console.log(epicPhrase)

// Template literals
let epicPhrase2 = `${hello} ${world}!`
console.log(epicPhrase2)

// Multi-lines strings
let lorem = 'esto es un string \ny aca sigue ese string'
console.log(lorem)

let lorem2 = `esto es un string 
y aca sigue ese string`
console.log(lorem2)